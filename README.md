# better-drive

2-way Google Drive sync with `.driveignore` filter — rclone engine embedded
in-process via librclone, Windows tray client.

Status: bootstrap (Task 0 — harness readiness spike). See `cmd/spike/` for the
librclone engine spike proving the Drive-only backend build compiles and the
`core/version` RPC call succeeds.
